version: 2

models:
  - name: stg_payments
    description: Staging table of payments
    columns:
      - name: order_id
        description: Relationship to PK for orders
        tests:
          - relationships:
                to: ref('stg_orders')
                field: order_id